// Groundwork — shared Supabase credentials.
// Fill these in ONCE here; every page on the site (homepage + every article)
// loads this file and reuses the same values. Per-article settings (like
// ARTICLE_ID) stay in each article's own <script> — only the project-level
// URL and key live here.
var CONFIG_SHARED = {
  SUPABASE_URL: 'https://gmcbnnsgpqvboatqjszb.supabase.co',
  SUPABASE_ANON_KEY: 'sb_publishable_ta8TNaIyn-0XHyqn3Cfh7A_iQ2sgvFu'
};
